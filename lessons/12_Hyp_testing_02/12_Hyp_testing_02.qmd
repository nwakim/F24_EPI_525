---
title: "Lesson 12: Inference for mean difference from two-sample dependent/paired data"
subtitle: "TB sections 5.2"
author: "Meike Niederhausen and Nicky Wakim"
title-slide-attributes:
    data-background-color: "#3070BF"
date: "11/11/2024"
format: 
  revealjs:
    theme: "../simple_NW.scss"
    chalkboard: true
    slide-number: true
    show-slide-number: all
    width: 1955
    height: 1100
    footer: Lesson 12 Slides
    html-math-method: mathjax
    highlight-style: arrow
execute:
  echo: true
  freeze: auto
---

```{r}
#| label: "setup" 
#| include: false
#| message: false
#| warning: false

library(tidyverse)    
library(oibiostat)
library(janitor)
library(rstatix)
library(knitr)
library(gtsummary)
library(moderndive)
library(gt)
library(broom) # NEW!!
library(here)

# terminal: for icons
# quarto install extension quarto-ext/fontawesome

# set ggplot theme for slides 
theme_set(theme_bw(base_size = 22))
# theme_update(text = element_text(size=16))  # set global text size for ggplots

BodyTemps <- read.csv(here::here("data", "BodyTemperatures.csv"))
#                     location: look in "data" folder
#                               for the file "BodyTemperatures.csv"


set.seed(456)
```



## Where are we?

![](../img_slides/course_map.png){fig-align="center"}

## Last time: Inference for a single-sample mean

- Inference for a single-sample mean includes confidence intervals and hypothesis testing

- More on the intuition of this!!

```{css, echo=FALSE}
.reveal code {
  max-height: 100% !important;
}
```

## Reference: Steps in a Hypothesis Test

1.  Check the [**assumptions**]{style="color:#3070BF"}

2.  Set the [**level of significance**]{style="color:#3070BF"} $\alpha$

3.  Specify the [**null**]{style="color:#3070BF"} ( $H_0$ ) and [**alternative**]{style="color:#3070BF"} ( $H_A$ ) [**hypotheses**]{style="color:#3070BF"}

    1.  In symbols
    2.  In words
    3.  Alternative: one- or two-sided?

4.  Calculate the [**test statistic**]{style="color:#3070BF"}.

5.  Calculate the [**p-value**]{style="color:#3070BF"} based on the observed test statistic and its sampling distribution

6.  Write a [**conclusion**]{style="color:#3070BF"} to the hypothesis test

    1.  Do we reject or fail to reject $H_0$?
    2.  Write a conclusion in the context of the problem


## Last time: example of a hypothesis test for a single-sample mean

::::: pink
::: pink-ttl
Example of hypothesis test based on the 1992 JAMA data
:::

::: pink-cont
Is there evidence to support that the population mean body temperature is different from 98.6°F?
:::
:::::

:::::: columns
::: {.column width="50%"}
1.  **Assumptions:** The individual observations are independent and the number of individuals in our sample is 130. Thus, we can use CLT to approximate the sampling distribution.

4-5.
:::

::: {.column width="25%"}
2.  Set $\alpha = 0.05$
:::

::: {.column width="25%"}
3.  **Hypothesis:**

    \begin{aligned}
    H_0 &: \mu = 98.6\\
    \text{vs. } H_A&: \mu \neq 98.6
    \end{aligned}
:::
::::::

```{r}
temps_ttest <- t.test(x = BodyTemps$Temperature, mu = 98.6)
tidy(temps_ttest) %>% gt() %>% tab_options(table.font.size = 36)
```

6.  **Conclusion:** We reject the null hypothesis. The average body temperature in the sample was 98.25°F (95% CI 98.12, 98.38°F), which is discernibly different from 98.6°F ( $p$-value \< 0.001).


# Learning Objectives

1.  Define paired data and explain how it differs from independent samples in the context of statistical analysis.
2.  Construct confidence intervals for the mean difference in paired data and interpret these intervals in the context of the research question.
3.  Perform the appropriate hypothesis test for paired data.

# Learning Objective

## Different types of inference based on different data types

| Lesson | Section | Population parameter | Symbol (pop) | Point estimate | Symbol (sample) | SE |
|:------------|:------------|:------------|:------------|:------------|:------------|:----------|
| 11 | 5.1 | Pop mean | $\mu$ | Sample mean | $\overline{x}$ | $\frac{s}{\sqrt{n}}$ |
| 12 | 5.2 | Pop mean of paired diff | $\mu_d$ or $\delta$ | Sample mean of paired diff | $\overline{x}_{d}$ | [**???**]{style="color:red"} |
| 13 | 5.3 | Diff in pop means | $\mu_1-\mu_2$ | Diff in sample means | $\overline{x}_1 - \overline{x}_2$ |  |
| 15 | 8.1 | Pop proportion | $p$ | Sample prop | $\widehat{p}$ |  |
| 15 | 8.2 | Diff in pop prop's | $p_1-p_2$ | Diff in sample prop's | $\widehat{p}_1-\widehat{p}_2$ |  |

## What are paired data?

- Paired data: two sets of observations are uniquely paired so that an observation in one set matches an observation in the other

- Examples
  -   Enroll pairs of identical twins to study a disease
  -   Enroll parent and child pairs to study cholesterol levels
  -   Enroll people and collect data before & after an intervention (longitudinal data)
  -   [Textbook example: Compare maximal speed of competitive swimmers wearing a wetsuit vs. wearing a regular swimsuit]{style="color:green"}
    -   Will use these data on homework

- The paired data result in a natural measure of difference
  - Example: Enroll parent and child pairs to study cholesterol levels
    - We can look at the difference in cholesterol levels between parent and child
    
## For paired data: Population parameters vs. sample statistics

::: columns
::: {.column width="50%"}

[__Population parameter__]{style="color:#C83532;"}

- Mean difference: $\delta$ ("delta", lowercase)
- Standard deviation: $\sigma_d$ ("sigma")
- Variance: $\sigma_d^2$



:::

::: {.column width="50%"}
[__Sample statistic (point estimate)__]{style="color:#EF85B3;"}

- Sample mean difference: $\overline{x}_d$
- Sample standard deviation: $s_d$
- Sample variance: $s_d^2$



:::
:::

 

- Using $d$ helps us distinguish between a single sample and paired data

## Can a vegetarian diet change cholesterol levels?

-   [**We will illustrate how to perform a hypothesis test for paired data as we work through this example**]{style="color:#5BAFF8"}
-   **Scenario**:
    -   43 non-vegetarian people were enrolled in a study and were instructed to adopt a vegetarian diet
    -   Cholesterol levels were measured before and after the diet
-   **Question**: [Is there evidence to support that cholesterol levels changed after the vegetarian diet?]{style="color:purple"}
-   How to answer the question?
    -   First, calculate changes (differences) in cholesterol levels
        -   We usually do after - before if the data are longitudinal

:::::: columns
::: {.column width="30%"}
Calculate **CI for the\
mean difference** $\delta$:

$$\bar{x}_d \pm t^*\cdot\frac{s_d}{\sqrt{n}}$$
:::

::: {.column width="35%"}
Run a **hypothesis test**

Hypotheses

\begin{align}
H_0:& \delta = \delta_0 \\
H_A:& \delta \neq \delta_0 \\
(or&~ <, >)
\end{align}
:::

::: {.column width="35%"}
<br> Test statistic

$$
t_{\bar{x}_d} = \frac{\bar{x}_d - \delta_0}{\frac{s_d}{\sqrt{n}}}
$$
:::
::::::

## EDA: Explore the cholesterol data

-   Scenario:
    -   43 non-vegetarian people were enrolled in a study
    -   They were instructed to adopt a vegetarian diet
    -   Cholesterol levels were measured before and after the diet

```{r}
chol <- read_csv(here::here("data", "chol213_n40.csv"))
glimpse(chol)
chol %>% get_summary_stats(type = "common") %>% gt() %>% 
  tab_options(table.font.size = 40)
```

## EDA: Cholesterol levels before and after vegetarian diet

::::: columns
::: {.column width="50%"}
Before:
```{r}
#| fig.width: 10
#| fig.height: 3
ggplot(chol, aes(x=Before)) +
  geom_histogram()
ggplot(chol, aes(x=Before)) +
  geom_boxplot()
```
:::

::: {.column width="50%"}
After:
```{r}
#| fig.width: 10
#| fig.height: 3
ggplot(chol, aes(x=After)) +
  geom_histogram()
ggplot(chol, aes(x=After)) +
  geom_boxplot()
```
:::
:::::

## EDA: Spaghetti plot of cholesterol levels before & after diet

-   Visualize the individual before vs. after diet changes in cholesterol levels

[*What does this figure tell us?*]{style="color:#BF396F"}

```{r}
#| fig.width: 10
#| fig.height: 6
#| echo: false
chol_long <- chol %>% 
  # need an ID column for the plot
  # make it factor so that coloring is not on continuous scale
  mutate(ID = factor(1:n())) %>% 
  # make data long for plot: 
  pivot_longer(
    cols = Before:After,
    names_to = "Time",  # need a column for Before & After on x-axis
    values_to = "Cholesterol") %>% # need a column of all cholesterol values for y-axis
  mutate(
    # change Time a factor variable so that can reorder
    # levels so that Before is before After
    Time = factor(Time, levels = c("Before", "After"))
    )
  
ggplot(chol_long, 
       aes(x=Time, y = Cholesterol, 
           # need to include group = ID 
           # to create a line for each ID
           color = ID, group = ID)) + 
  geom_line(show.legend = FALSE)
```


## EDA: Differences in cholesterol levels: After - Before diet

- How do we calculate the difference in cholesterol levels?

```{r}
#| fig.width: 10
#| fig.height: 3
chol <- chol %>% 
  mutate(DiffChol = After - Before) 
glimpse(chol)
```


## Poll Everywhere Question 

```{r}
#| fig.width: 10
#| fig.height: 3
#| echo: false
#| eval: false


chol %>% get_summary_stats(type = "common") %>% 
  gt() %>% tab_options(table.font.size = 40)
```

## EDA: Differences in cholesterol levels: After - Before diet

::::: columns
::: {.column width="50%"}
Before: 

```{r}
#| fig.width: 10
#| fig.height: 3
ggplot(chol, aes(x=Before)) +
  geom_histogram()
```

After:
```{r}
#| fig.width: 10
#| fig.height: 3
ggplot(chol, aes(x=After)) +
  geom_histogram()
```
:::

::: {.column width="50%"}
Difference: 

```{r}
#| fig.width: 10
#| fig.height: 3
ggplot(chol, aes(x=DiffChol)) + 
  geom_histogram()
```
:::
:::::

## Use the same building blocks as the single-sample mean on the difference

- Establish this
- Then can talk about how the hypothesis test is basically the same after that

# Learning Objective

## Reference: Steps in a Hypothesis Test

1.  Check the [**assumptions**]{style="color:#3070BF"}

2.  Set the [**level of significance**]{style="color:#3070BF"} $\alpha$

3.  Specify the [**null**]{style="color:#3070BF"} ( $H_0$ ) and [**alternative**]{style="color:#3070BF"} ( $H_A$ ) [**hypotheses**]{style="color:#3070BF"}

    1.  In symbols
    2.  In words
    3.  Alternative: one- or two-sided?

4.  Calculate the [**test statistic**]{style="color:#3070BF"}.

5.  Calculate the [**p-value**]{style="color:#3070BF"} based on the observed test statistic and its sampling distribution

6.  Write a [**conclusion**]{style="color:#3070BF"} to the hypothesis test

    1.  Do we reject or fail to reject $H_0$?
    2.  Write a conclusion in the context of the problem

## Step 1: Check the [**assumptions**]{style="color:#3070BF"}

-   The assumptions to run a hypothesis test on a sample are:

    -   **Independent pairs**: Each pair is independent from all other pairs,
    -   **Approximately normal sample or big n**: the distribution of the sample should be approximately normal, *or* the sample size should be at least 30

 

-   These are the criteria for the Central Limit Theorem in Lesson 09: Variability in estimates

 

-   In our example, we would check the assumptions with a statement:

    -   The pairs of observations are independent from each other and the number of pairs in our sample is 40. Thus, we can use CLT to approximate the sampling distribution.

## Step 2: Set the [**level of significance**]{style="color:#3070BF"} $\alpha$

-   **Before doing a hypothesis test**, we set a cut-off for how small the $p$-value should be in order to reject $H_0$.

-   Typically choose $\alpha = 0.05$

 

- See Lesson 11: Hypothesis Testing 1: Single-sample mean

## Step 3: Null & Alternative Hypotheses (1/2)

In statistics, a **hypothesis** is a statement about the value of an **unknown population parameter**.

A [**hypothesis test**]{style="color:#3070BF"} consists of a test between two competing hypotheses:

1.  a [**null**]{style="color:#3070BF"} hypothesis $H_0$ (pronounced “H-naught”) vs.
2.  an [**alternative**]{style="color:#3070BF"} hypothesis $H_A$ (also denoted $H_1$)

Example of hypotheses in words:

\begin{aligned}
H_0 &: \text{The population mean difference in cholesterol levels after a vegetarian diet is zero}\\
\text{vs. } H_A &: \text{The population mean difference in cholesterol levels after a vegetarian diet is different than zero}
\end{aligned}

1.  $H_0$ is a claim that there is "no effect" or "no difference of interest."
2.  $H_A$ is the claim a researcher wants to establish or find evidence to support. It is viewed as a "challenger" hypothesis to the null hypothesis $H_0$

## Step 3: Null & Alternative Hypotheses (2/2)

::::::::::: columns
:::::: column
::::: blue
::: blue-ttl
Notation for hypotheses (for paired data)
:::

::: blue-cont
\begin{aligned}
H_0 &: \delta = \delta_0\\
\text{vs. } H_A&: \delta \neq, <, \textrm{or}, > \delta_0
\end{aligned}
:::
:::::
::::::

:::::: column
::::: pink
::: pink-ttl
Hypotheses test for example
:::

::: pink-cont
\begin{aligned}
H_0 &: \delta = 0\\
\text{vs. } H_A&: \delta \neq 0
\end{aligned}
:::
:::::
::::::
:::::::::::

We call $\delta_0$ the *null value* (hypothesized population mean from $H_0$)

:::::::: columns
::: {.column width="32%"}
$H_A: \delta \neq \delta_0$

-   not choosing a priori whether we believe the population mean is greater or less than the null value $\delta_0$
:::

::: {.column width="2%"}
:::

::: {.column width="32%"}
$H_A: \delta < \delta_0$

-   believe the population mean is **less** than the null value $\delta_0$
:::

::: {.column width="2%"}
:::

::: {.column width="32%"}
$H_A: \delta > \delta_0$

-   believe the population mean is **greater** than the null value $\delta_0$
:::
::::::::

-   $H_A: \delta \neq \delta_0$ is the most common option, since it's the most conservative

## Step 4: Test statistic

```{r}
chol %>% select(DiffChol) %>% get_summary_stats(type = "common") %>% gt() %>% tab_options(table.font.size = 40)
```

::::: columns
::: {.column width="60%"}
$$
t_{\bar{x}_d} = \frac{\bar{x}_d - \delta_0}{\frac{s_d}{\sqrt{n}}}
$$

-   [*Calculate the test statistic.*]{style="color:#BF396F"}
-   [Based on the value of the test statistic, do you think we are going to reject or fail to reject $H_0$?]{style="color:#BF396F"}
-   [What probability distribution does the test statistic have?]{style="color:#BF396F"}
-   [Are the **assumptions** for a paired t-test satisfied so that we can use the probability distribution to calculate the $p$-value??]{style="color:#BF396F"}
:::

::: {.column width="40%"}
```{r}
#| include: false
n <- 43
alpha <- 0.05
mu <- 0
(p_area <- 1-alpha/2)
(xbar <- mean(chol$DiffChol))
(sd <- sd(chol$DiffChol))
(se <- sd/sqrt(n))

(tstat <- (xbar - mu)/se)
1-pt(tstat, df=n-1)
2*(1-pt(tstat, df=n-1))
```
:::
:::::

## Step 5: p-value

The [**p-value**]{style="color:#BF396F"} is the **probability** of obtaining a test statistic *just as extreme or more extreme* than the observed test statistic assuming the null hypothesis $H_0$ is true.

::::: columns
::: {.column width="50%"}
```{r}
#| fig.width: 10
#| fig.height: 4
#| echo: false
# specify upper and lower bounds of shaded region below
mu <- 0
std <- se

# The following figure is only an approximation of the 
# sampling distribution since I used a normal instead
# of t-distribution to make it.

ggplot(data.frame(x = c(mu-6*std, mu+6*std)), aes(x = x)) + 
  stat_function(fun = dnorm, 
                args = list(mean = mu, sd = std)) + 
  scale_y_continuous(breaks = NULL) +
  scale_x_continuous(breaks=c(mu, mu - 3.4*(1:6), mu + 3.4*(1:6))) +
  theme(axis.text.x=element_text(angle = -30, hjust = 0)) +
  labs(y = "", 
       x = "sample mean difference",
       title = "Sampling distribution of mean difference") +
  geom_vline(xintercept = c(-xbar, xbar), 
             color = "red")
```

```{r}
#| fig.height: 3
#| fig.width: 10
#| echo: false
ggplot(data = data.frame(x = c(-6, 6)), aes(x)) + 
  stat_function(fun = dt, args = list(df = n-1)) + 
  ylab("") + 
  xlab("t-dist with df = 23") +
  scale_y_continuous(breaks = NULL) + 
  scale_x_continuous(breaks=c(mu, mu - (1:5), mu + (1:5))) +
  geom_vline(xintercept = c(-tstat,tstat), 
             color = "red")
```
:::

::: {.column width="50%"}
[Calculate the *p*-value and shade in the area representing the p-value:]{style="color:#BF396F"}

```{r}
#| include: false
2*pt(-5.4548, df = 130-1, 
     lower.tail = TRUE)

(tstat <- (xbar - mu)/se)
1-pt(tstat, df=n-1)
(pv <- 2*(1-pt(tstat, df=n-1)))
2*(pt(-tstat, df=n-1))
```
:::
:::::

## Step 6: Conclusion to hypothesis test

\begin{align}
H_0:& \delta = 0 \\
H_A:& \delta \neq 0 \\
\end{align}

-   Recall the $p$-value = $8.434775 \cdot 10 ^{-6}$
-   Use $\alpha$ = 0.05.
-   [Do we reject or fail to reject $H_0$?]{style="color:#BF396F"}

**Conclusion statement**:

-   Stats class conclusion
    -   There is sufficient evidence that the (population) mean difference in cholesterol levels after a vegetarian diet is different from 0 mg/dL ( $p$-value \< 0.001).
-   More realistic manuscript conclusion:
    -   After a vegetarian diet, cholesterol levels decreased by on average 19.54 mg/dL (SE = 3.43 mg/dL, 2-sided $p$-value \< 0.001).

# Learning Ocjective


## 95% CI for the mean difference in cholesterol levels

```{r}
chol %>% 
  select(DiffChol) %>% 
  get_summary_stats(type = "common") %>% 
  gt() %>% tab_options(table.font.size = 40)
```

CI for $\mu_d$ (or $\delta$): [*How was* $t^*$ calculated?]{style="color:#BF396F"}

```{r}
#| include: false
n <- 43
alpha <- 0.05
(p_area <- 1-alpha/2)
(xbar <- mean(chol$DiffChol))
(sd <- sd(chol$DiffChol))
(tstar <- qt(p_area, df=n-1))  # df = n-1
(se <- sd/sqrt(n))
(moe <- tstar * se) 
(LB <- xbar - moe)
(UB <- xbar + moe)
```

::::: columns
::: {.column width="50%"}
\begin{align}
\bar{x}_d &\pm t^*\cdot\frac{s_d}{\sqrt{n}}\\
`r round(xbar,3)` &\pm `r round(tstar,3)`\cdot\frac{`r round(sd,3)`}{\sqrt{`r n`}}\\
`r round(xbar,3)` &\pm `r round(tstar,3)`\cdot `r round(se,3)`\\
`r round(xbar,3)` &\pm `r round(moe,3)`\\
(`r round(LB, 3)`&, `r round(UB, 3)`)
\end{align}
:::

::: {.column width="50%"}
Conclusion:\
We are 95% that the (population) mean difference in cholesterol levels after a vegetarian diet is between `r round(LB, 3)` mg/dL and `r round(UB, 3)` mg/dL.

-   [*Based on the CI, is there evidence the diet made a difference in cholesterol levels? Why or why not?*]{style="color:#BF396F"}
:::
:::::

# Running a paired t-test in R

## R option 1: Run a 1-sample `t.test` using the paired differences

$H_A: \delta \neq 0$

```{r}
t.test(x = chol$DiffChol, mu = 0)
```

[*Run the code without `mu = 0`. Do the results change? Why or why not?*]{style="color:#BF396F"}

## R option 2: Run a 2-sample `t.test` with `paired = TRUE` option

$H_A: \delta \neq 0$

-   For a 2-sample t-test we specify both `x=` and `y=`
-   Note: `mu = 0` is the default value and doesn't need to be specified

```{r}
t.test(x = chol$Before, y = chol$After, mu = 0, paired = TRUE)
```

[*What is different in the output compared to option 1?*]{style="color:#BF396F"}

## R option 3: Run a 2-sample `t.test` with `paired = TRUE` option, but using the long data and a "formula" (1/2)

::::: columns
::: {.column width="60%"}
-   The data have to be in a `long` format for option 3, where each person has 2 rows: one for Before and one for After.
    -   The long dataset `chol_long` was created for the slide "EDA: Spaghetti plot of cholesterol levels before & after diet".
    -   See the code to create it there.
-   [*What information is being stored in each of the columns?*]{style="color:#BF396F"}
:::

::: {.column width="40%"}
```{r}
# first 16 rows of long data:
head(chol_long, 16)
```
:::
:::::

## R option 3: Run a 2-sample `t.test` with `paired = TRUE` option, but using the long data and a "formula" (2/2)

:::::: columns
:::: {.column width="40%"}
::: {style="font-size: 90%;"}
-   Use the usual `t.test`
-   What's different is that
    -   instead of specifying the variables with `x=` and `y=`,
    -   we give a **formula** of the form `y ~ x` using *just the variable names*,
    -   and then specify the name of the dataset using `data =`
-   This method is often used in practice, and more similar to the coding style of running a regression model (BSTA 512 & 513)
:::
::::

::: {.column width="60%"}
```{r}
# using long data 
# with columns Cholesterol & Time
# t.test(Cholesterol ~ Time, 
#        paired = TRUE, 
#        data = chol_long)
```

-   [*What is different in the output compared to option 1?*]{style="color:#BF396F"}
-   [*Rerun the test using `Time ~ Cholesterol` (switch the variables). What do you get?*]{style="color:#BF396F"}
:::
::::::

## Compare the 3 options

-   [*How is the code similar and different for the 3 options?*]{style="color:#BF396F"}
-   [*Given a dataset, how would you choose which of the 3 options to use?*]{style="color:#BF396F"}

```{r}
# option 1
t.test(x = chol$DiffChol, mu = 0) %>% tidy() %>% gt() %>% tab_options(table.font.size = 40)# tidy from broom package
# option 2
t.test(x = chol$Before, y = chol$After, mu = 0, paired = TRUE) %>% tidy() %>% gt() %>% tab_options(table.font.size = 40)
# option 3
#t.test(Cholesterol ~ Time, paired = TRUE, data = chol_long) %>% tidy() %>% gt() %>% tab_options(table.font.size = 40)
```

## What if we wanted to test whether the diet *decreased* cholesterol levels?

::::: columns
::: {.column width="50%"}
[What changes in each of the steps?]{style="color:#BF396F"}

1.  Set the [**level of significance**]{style="color:#BF396F"} $\alpha$

2.  Specify the [**hypotheses**]{style="color:#BF396F"} $H_0$ and $H_A$

    -   Alternative: one- or two-sided?

3.  Calculate the [**test statistic**]{style="color:#BF396F"}.

4.  Calculate the [**p-value**]{style="color:#BF396F"} based on the observed test statistic and its sampling distribution

5.  Write a [**conclusion**]{style="color:#BF396F"} to the hypothesis test
:::

::: {.column width="50%"}
:::
:::::

## R: What if we wanted to test whether the diet *decreased* cholesterol levels?

-   [*Which of the 3 options to run a paired t-test in R is being used below?*]{style="color:#BF396F"}
-   [*How did the code change to account for testing a decrease in cholesterol levels?*]{style="color:#BF396F"}
-   [*Which values in the output changed compared to testing for a change in cholesterol levels? How did they change?*]{style="color:#BF396F"}

```{r}
# alternative = c("two.sided", "less", "greater")
t.test(x = chol$DiffChol, mu = 0, alternative = "less") %>% 
  tidy() %>% 
  gt() %>% tab_options(table.font.size = 40)
```

## One-sided confidence intervals

:::::: columns
::: {.column width="40%"}
Formula for a **2-sided** (1- $\alpha$ )% **CI**:

$$\bar{x} \pm t^*\cdot\frac{s}{\sqrt{n}}$$

-   $t^*$ = `qt(1-alpha/2, df = n-1)`
-   $\alpha$ is split over both tails of the distribution
:::

::: {.column width="3%"}
:::

::: {.column width="57%"}
A **one-sided** (1- $\alpha$ )% **CI** has all (1- $\alpha$ )% on just the left or the right tail of the distribution:

\begin{align}
(\bar{x} - t^*\cdot\frac{s}{\sqrt{n}},~\infty) \\
(\infty,~\bar{x} + t^*\cdot\frac{s}{\sqrt{n}})
\end{align}

-   $t^*$ = `qt(1-alpha, df = n-1)` for a\
    1-sided lower (1- $\alpha$ )% CI
-   $t^*$ = `qt(alpha, df = n-1)` for a 1-sided upper (1- $\alpha$ )% CI
-   A 1-sided CI gives estimates for a lower or upper bound of the population mean.
-   See Section 4.2.3 of the V&H book for more
:::
::::::

## [Today]{style="color:green"} & [what's next?]{style="color:darkblue"}

::: {style="font-size: 90%;"}
CI's and hypothesis tests for different scenarios:

$$\text{point estimate} \pm z^*(or~t^*)\cdot SE,~~\text{test stat} = \frac{\text{point estimate}-\text{null value}}{SE}$$

| Day | Book | Population <br> parameter | Symbol | Point estimate | Symbol | SE |
|-----------|-----------|-----------|-----------|-----------|-----------|-----------|
| 10 | 5.1 | [Pop mean]{style="color:green"} | [$\mu$]{style="color:green"} | [Sample mean]{style="color:green"} | [$\bar{x}$]{style="color:green"} | [$\frac{s}{\sqrt{n}}$]{style="color:red"} |
| 10 | 5.2 | [Pop mean of paired diff]{style="color:green"} | [$\mu_d$ or $\delta$]{style="color:green"} | [Sample mean of paired diff]{style="color:green"} | [$\bar{x}_{d}$]{style="color:green"} | [$\frac{s_d}{\sqrt{n}}$]{style="color:red"} |
| 11 | 5.3 | [Diff in pop <br> means]{style="color:darkblue"} | [$\mu_1-\mu_2$]{style="color:darkblue"} | [Diff in sample <br> means]{style="color:darkblue"} | [$\bar{x}_1 - \bar{x}_2$]{style="color:darkblue"} | [**???**]{style="color:red"} |
| 12 | 8.1 | Pop proportion | $p$ | Sample prop | $\widehat{p}$ |  |
| 12 | 8.2 | Diff in pop <br> proportions | $p_1-p_2$ | Diff in sample <br> proportions | $\widehat{p}_1-\widehat{p}_2$ |  |
:::
